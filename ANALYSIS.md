# 원본 분석 및 새 실행 경로

검토 기준: [frankee0920-rgb/rhino-architectural-reverse-modeling](https://github.com/frankee0920-rgb/rhino-architectural-reverse-modeling), 로컬 원본 commit `9249b95503a9e9f843279b75770f1f41ba4e418e`. 원본 `README.md`, `SKILL.md`, `TOOLING.md`, `references/`, `tools/`를 확인했습니다.

## 확인된 구조와 약점

1. 원본은 **Codex/Claude Code 작업 지침**입니다. `.3dm` 모델을 만드는 자체 서버나 생성 엔진은 포함하지 않습니다. README는 기본 Rhino 경로에 별도 Rhino MCP가 필요하다고 명시합니다. 따라서 스킬 설치 성공만으로 모델 생성이 되지는 않습니다.
2. 기본 실행 경로가 Rhino MCP와 Rhino 내부 스크립트에 묶여 있습니다. 이 PC에서는 Rhino의 라이선스 상태 때문에 `RunPythonScript`가 차단된 기록이 있으므로, 원본 기본 경로의 작업 능력을 사용할 수 없습니다. 이는 원본 저장소의 코드 오류로 확인된 것은 아닙니다.
3. 원본 보조 Python 파일 네 개는 이미지 준비·비교·검수 자료 작성 도구입니다. 독립된 건축 지오메트리 생성기는 아닙니다.
4. 원본은 실제 저장 모델과 두 시점 렌더를 요구합니다. 하지만 저장·렌더를 수행할 외부 애플리케이션/MCP의 가용성을 보장하지 않습니다. 사진 한 장에서 숨은 형상과 절대 치수를 고유하게 복원할 수 없다는 한계는 원본도 명시합니다.
5. 확인한 원본 파일 목록에는 실행 테스트와 재현 가능한 종속성 잠금 파일이 없습니다. 이는 설치·작동 검증을 사용 환경별로 반복해야 한다는 의미이며, 저장소 자체가 반드시 실패한다는 뜻은 아닙니다.

## 변경한 구조

| 원본의 요구 | 새 구현 |
|---|---|
| 사진·치수·설명에서 형상 판단 | `SKILL.md`의 증거 구분과 JSON 구성요소 명세. 사진 자동 계측은 사람/에이전트가 검토 |
| Rhino MCP로 모델 작성 | `rhino3dm`으로 Rhino 없이 `.3dm` 직접 작성. 복잡한 형상은 Blender `.blend`를 메시로 변환해 가져오기 |
| 편집 가능한 구성요소 | 이름·레이어·색상·재질·근거·원본 제어 JSON을 구성요소별 객체에 저장 |
| 저장 파일 확인 | `.3dm`을 다시 열고 객체·레이어·단위·범위를 반환 |
| 두 시점 이미지 | 저장된 메시를 읽어 직교 검수 PNG 생성. 기존 Blender가 있으면 두 렌더 PNG와 `.blend`도 생성 |
| 이미지 비교·검수 참고 | 원본 MIT 보조 도구와 참고 자료를 `resources/`에 보존 |

`rhino3dm`은 [McNeel이 Rhino와 독립적으로 3DM을 읽고 쓰도록 제공하는 오픈소스 라이브러리](https://developer.rhino3d.com/guides/opennurbs/what-is-rhino3dmio/)입니다. 따라서 이 구현은 Rhino 라이선스 검사를 우회하지 않고, Rhino를 실행하지 않습니다. Blender는 선택 기능이며 기존 설치 버전 4.5를 사용해 검증했습니다.

## 아직 동등하지 않은 부분

- JSON 생성기는 상자, 개구부 벽, 박공지붕 중심입니다. Blender 장면 가져오기로 더 다양한 메시 형상을 다룰 수 있으나, `.blend` 편집 이력과 카메라/조명/노드 재질은 `.3dm`에 보존되지 않습니다. Rhino의 NURBS, Boolean, 정확한 솔리드 연산, 현재 문서/뷰포트 제어와 동등하지 않습니다.
- `.3dm` 안의 객체는 구성요소별로 편집 가능하고 JSON으로 수치 재생성이 가능하지만, Rhino 네이티브 NURBS 객체처럼 자유로운 곡면 제어는 아닙니다.
- 단색 재질·예시 조명의 Blender 렌더는 실제 최종 메시에서 나왔지만 원본 사진의 카메라·재질·조명을 자동 재현하지 않습니다.
- 건축적 정확도는 입력 치수와 해석에 좌우됩니다. 예제 주택은 기능 검증용이며 원본 사진과의 일치·시공 가능성을 입증하지 않습니다.

## 실제 검증

- 격리 Python 3.12 환경에서 `rhino3dm==8.35.0`, `mcp==1.30.0`, `Pillow==11.3.0`, `numpy==2.3.3` 설치.
- 예제 `.3dm` 저장 후 재읽기: 13개 객체, 4개 레이어, 미터 단위, 색상·재질·근거 문자열 확인.
- 직교 PNG 두 장을 저장된 `.3dm` 메시에서 생성하고 실제 시각 검수.
- Blender 4.5 CLI 및 MCP에서 각각 두 장의 렌더와 `.blend` 생성 확인.
- MCP stdio의 10개 도구 목록·생성·재읽기·렌더·Blender 장면 가져오기 호출 및 작업 폴더 이탈 요청 거부 확인.
- 3DM → BLEND → 3DM 왕복에서 13개 건축 객체와 4개 레이어를 유지하고 렌더용 보조 바닥을 제외했습니다. 가져온 모델의 앞·뒤 PNG를 직접 시각 검수했습니다.
- Blender 모디파이어를 적용한 곡면 기둥과 반전된 객체를 별도 장면에서 가져와 192개 정점·380개 면의 읽기 가능한 유효 3DM 메시를 확인했습니다.
- `python -m unittest discover -s tests -v` 전체 테스트 통과(설치 충돌 방지 테스트 포함). 도전형 점수는 복원 JSON이 아닌 저장된 3DM 메시 정점으로 재계산했고, JSON 사후 변경에 영향을 받지 않는 회귀 테스트를 추가했습니다.

원본 저작권/허가 고지는 `UPSTREAM_LICENSE.txt`에 보존했습니다. 이 문서는 기술적 기능 경계의 기록입니다.
